{
  "name": "amazon-book-research-helper",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watchjs": "coffee -w -o build src",
    "watchcss": "stylus -w -o build src",
    "release": "rm -f /tmp/ext.zip && zip -r /tmp/ext.zip manifest.json third-party build && ls -l /tmp/ext.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "coffee-script": "1.*",
    "stylus": "0.*"
  }
}
